#include "main.h"

/**
* _islower - function to chec if character is lowercase
* @c: checks input of function
* Return: return 1 if c is lowercase
* Otherwise Always 0 success
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
