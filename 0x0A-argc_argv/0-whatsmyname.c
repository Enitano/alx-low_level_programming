#include "main.h"
#include <stdio.h>

/**
 * prog_name - prints name of program
 * @argc: argument couunt
 * @argv: vector or array of arguments
 * Return: 0
 *i

int prog_name(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
