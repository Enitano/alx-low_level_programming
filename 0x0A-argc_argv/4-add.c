#include <stdio.h>

/**
 * main - adds positive numbers
 * @argc: size of args
 * @argv: array of args
 * Return: 0 success, 1 otherwise
 */

int main(int argc, char argv)
{
	int index, sum;

	if (argc <= 1)
