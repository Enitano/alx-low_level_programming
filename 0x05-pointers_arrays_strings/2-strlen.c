#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to check
 * Return: void
 */

int _strlen(char *s)
{
	int str;

	str = 0;
	while (s[str] != '\0')
	{
		str++
	}
	return (str);
}
